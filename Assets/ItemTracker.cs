using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class ItemTracker : MonoBehaviour
{
    [SerializeField] private Slider slider;
    [SerializeField] private TMP_Text text;

    [SerializeField] private ItemSender sender;

    private void Update()
    {
    }
}
